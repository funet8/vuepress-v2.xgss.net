# 使用免费的 Hugging Face 服务器部署 OpenClaw，零成本玩转开源大龙虾（报错）



在当下的开源 AI Agent 圈，OpenClaw 绝对是自带话题度的存在，这款被大家亲切称为 “开源大龙虾” 的工具，凭借强大的功能圈粉无数，能实现发邮件、订机票、写代码等多种操作。

但很多人不知道的是，我们可以借助 Hugging Face 提供的免费云服务器（2 核 CPU+16G 内存 + 50G 硬盘）完成 OpenClaw 的部署，全程零成本，轻松解锁这款强大的开源工具。在开始部署前，我们先简单了解下这只 “大龙虾” 的由来，也能让大家对项目有更清晰的认知。



接下来进入正题，教大家一步步在 Hugging Face 的免费服务器上部署 OpenClaw，整个过程基于 Docker 实现，操作难度适中，技术小白也能跟着步骤完成。



## 一、准备工作：登录 / 注册 Hugging Face 账号

首先访问 Hugging Face 官网：

```
huggingface.co  复制到浏览器中
```

详细的注册方法可以参考之前星哥写的 [免费领取huggingface的2核16G云服务器，超简单教程](https://mp.weixin.qq.com/s/hLoS7w12dcI8NilrtwHLyg)

如果已有账号，直接点击页面中的「Log In」，输入用户名 / 邮箱和密码完成登录。

若无账号，点击「Sign Up」完成注册，注册流程简单，按页面提示填写信息即可，这是后续部署操作的基础。



## 二、创建 Space：搭建部署基础环境

进入 HuggingFace Spaces

### 1.新建Space

登录成功后，在页面中找到「New」按钮，点击后选择「Space」，进入 Space 创建页面。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772185945688.png?tx)



### 2.填写 Space 基础信息

输入「space name」（示例为 openclaw，可自定义），「Short description」为选填项，可暂时不填。

SDK: 选择 Docker

Template: 选择 Blank

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186001337.png?tx)

选择部署核心配置：SDK 类型选择「Docker」，Docker 模板默认选中「Blank」无需修改；Space hardware 默认选择免费配置，「Public」选项保持默认勾选，这样部署后的应用可实现公网访问。

### 3.创建Space

所有配置确认无误后，点击页面下方的「Create Space」，完成基础环境的创建。

Privacy: 选择 Private

![img](H:\typora_images\QQ_1772247821280.png)



## 三、新建Dataset

从个人头像页面，点击 + New Dataset

![img](H:\typora_images\QQ_1772247907662.png)



Privacy: 选择 Private

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772247977483.png?tx)

## 四、新建 Access Token

从个人头像页面，点击 Access Tokens

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772248073512.png?tx)

点击 Create new token

![img](H:\typora_images\QQ_1772248144260.png)

Token Type 选择 Write（因为要写入）

![img](H:\typora_images\QQ_1772248218179.png)



复制保存，下面要用。

![img](H:\typora_images\QQ_1772248194530-1772248202545.png)



## 五、配置Space变量

回到刚刚创建的 Space，右上角点击 `Settings`，拉到最下面找到 `Variables and Secrets`

变量

点击 New variable 创建

| 变量名          | 变量值                                             |
| --------------- | -------------------------------------------------- |
| OPENAI_API_BASE | https://integrate.api.nvidia.com/v1                |
| MODEL           | z-ai/glm4.7                                        |
| HF_DATASET      | 用户名/数据集名， 例如星哥的是： star0098/openclaw |

![img](H:\typora_images\QQ_1772248995376.png)

私有变量

点击 New secret 创建

| 变量名                    | 变量值                            |
| ------------------------- | --------------------------------- |
| OPENAI_API_KEY            | 对应nvidia的OPENAI_API_BASE的密钥 |
| HF_TOKEN                  | 创建的Access Tokens               |
| OPENCLAW_GATEWAY_PASSWORD | OpenClaw UI 的密码，自己设置      |



```
变量：

OPENAI_API_BASE：如果是用的 Nvidia NIM，那就是https://integrate.api.nvidia.com/v1;
MODEL：模型ID，比如z-ai/glm4.7；
HF_DATASET：刚刚新建的数据集，命名格式为 用户名/数据集名
密钥：

OPENAI_API_KEY：对应上面OPENAI_API_BASE的密钥；
HF_TOKEN：2.3 部分创建的Access Tokens
OPENCLAW_GATEWAY_PASSWORD：连接 OpenClaw UI 的密码，自己设置，后面会用到。
```



## 六、新建三个文件

回到创建的 Space，新建三个文件,

依次点击，Files ，Create new file

![img](H:\typora_images\QQ_1772249182357.png)

### sync.py

sync.py：负责和 Dataset 同步数据，实现配置文件持久化

```
import os
import sys
import tarfile
from huggingface_hub import HfApi, hf_hub_download

api = HfApi()
repo_id = os.getenv("HF_DATASET")
token = os.getenv("HF_TOKEN")
FILENAME = "latest_backup.tar.gz"

def restore():
    try:
        if not repo_id or not token:
            print("Skip Restore: HF_DATASET or HF_TOKEN not set")
            return
        
        # 直接下载最新文件
        print(f"Downloading {FILENAME} from {repo_id}...")
        path = hf_hub_download(repo_id=repo_id, filename=FILENAME, repo_type="dataset", token=token)
        
        with tarfile.open(path, "r:gz") as tar:
            tar.extractall(path="/root/.openclaw/")
        print(f"Success: Restored from {FILENAME}")
        return True
    except Exception as e:
        # 如果是第一次运行，仓库里没文件，报错是正常的
        print(f"Restore Note: No existing backup found or error: {e}")

def backup():
    try:
        if not repo_id or not token:
            print("Skip Backup: HF_DATASET or HF_TOKEN not set")
            return

        with tarfile.open(FILENAME, "w:gz") as tar:
            # 备份关键数据
            paths_to_backup = [
                "/root/.openclaw/sessions",
                "/root/.openclaw/agents/main/sessions",
                "/root/.openclaw/openclaw.json"
            ]
            for p in paths_to_backup:
                if os.path.exists(p):
                    arcname = p.replace("/root/.openclaw/", "")
                    tar.add(p, arcname=arcname)
        
        # 上传并覆盖
        api.upload_file(
            path_or_fileobj=FILENAME,
            path_in_repo=FILENAME,
            repo_id=repo_id,
            repo_type="dataset",
            token=token
        )
        print(f"Backup {FILENAME} Success (Overwritten).")
    except Exception as e:
        print(f"Backup Error: {e}")

if __name__ == "__main__":
    if len(sys.argv) > 1 and sys.argv[1] == "backup":
        backup()
    else:
        restore()

```

### start-openclaw.sh

start-openclaw.sh：启动和运行 openclaw

```
#!/bin/bash
set -e

# 1. 补全目录
mkdir -p /root/.openclaw/agents/main/sessions
mkdir -p /root/.openclaw/credentials
mkdir -p /root/.openclaw/sessions

# 2. 执行恢复
python3 /app/sync.py restore

# 3. 处理 API 地址
CLEAN_BASE=$(echo "$OPENAI_API_BASE" | sed "s|/chat/completions||g" | sed "s|/v1/|/v1|g" | sed "s|/v1$|/v1|g")

# 4. 生成配置文件
cat > /root/.openclaw/openclaw.json <<EOF
{
  "models": {
    "providers": {
      "nvidia": {
        "baseUrl": "$CLEAN_BASE",
        "apiKey": "$OPENAI_API_KEY",
        "api": "openai-completions",
        "models": [
          { "id": "$MODEL", "name": "$MODEL", "contextWindow": 128000 }
        ]
      }
    }
  },
  "agents": { "defaults": { "model": { "primary": "nvidia/$MODEL" } } },
  "commands": {
    "restart": true
  },
  "gateway": {
    "mode": "local", "bind": "lan", "port": $PORT,
    "trustedProxies": ["0.0.0.0/0"],
    "auth": { "mode": "token", "token": "$OPENCLAW_GATEWAY_PASSWORD" },
    "controlUi": { "allowInsecureAuth": true }
  }
}
EOF

# 5. 启动定时备份 (每 1 小时)
(while true; do sleep 3600; python3 /app/sync.py backup; done) &

# 6. 运行
openclaw doctor --fix
exec openclaw gateway run --port $PORT

```



### Dockerfile

Dockerfile：容器化环境配置

```
FROM node:22-slim

# 1. 基础依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    git openssh-client build-essential python3 python3-pip \
    g++ make ca-certificates && rm -rf /var/lib/apt/lists/*

RUN pip3 install --no-cache-dir huggingface_hub --break-system-packages

# 2. 安装 OpenClaw
RUN npm install -g openclaw@latest --unsafe-perm

# 3. 设置工作目录并拷贝脚本
WORKDIR /app
COPY sync.py .
COPY start-openclaw.sh .
RUN chmod +x start-openclaw.sh

# 4. 环境变量
ENV PORT=7860 HOME=/root

EXPOSE 7860
CMD ["./start-openclaw.sh"]

```



创建好三个文件，打好 commit，space 会自动触发构建，执行代码。

### 查看日志

![img](H:\typora_images\QQ_1772249409197.png)



看到上方日志，恭喜你，你的openclaw已成功启动！

打开你的 Space 地址看看吧：

格式为 https://{user}-{space}.hf.space/























## 三、配置 README.md：设置应用端口



### 1.编辑README.md

Space 创建完成后，进入对应的工作台，点击左侧的「Files」，找到 README.md 文件并点击进入编辑页面。

![img](H:\typora_images\QQ_1772186134623.png)



在文件的 metadata 配置区域添加应用端口配置`app_port: 18789`，完整的基础配置如下：

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186245221.png?tx)

```
---
title: OpenClaw
emoji: 🦀
colorFrom: red
colorTo: green
sdk: docker
app_port: 18789
pinned: false
---
```

配置完成后，下拉页面点击「Commit changes to main」提交修改，这一步是为了让后续部署的 OpenClaw 能通过指定端口访问。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186373456.png?tx)

### 2.创建 Dockerfile：编写核心部署脚本

Dockerfile 是 OpenClaw 部署的核心，负责配置运行环境、安装依赖、编写启动脚本等，具体操作步骤如下：

在 Space 工作台的「Files」页面，点击「Contribute」，选择「Create a new file」。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186522135.png?tx)



输入文件名「Dockerfile」，在内容区域粘贴以下核心脚本，脚本已做好完整的环境配置和启动逻辑：

```
FROM node:22-slim

# Install system dependencies
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

# Install OpenClaw globally
RUN npm install -g openclaw@latest

# Create config directory
RUN mkdir -p /root/.openclaw

# Set up environment for Hugging Face Spaces
ENV PORT=18789
ENV OPENCLAW_GATEWAY_MODE=local
ENV OPENCLAW_GATEWAY_PASSWORD=${OPENCLAW_GATEWAY_PASSWORD}

# Expose the port (Hugging Face default)
EXPOSE 18789

# Create a startup script that generates config and starts OpenClaw
RUN echo '#!/bin/bash\n\
set -x\n\
\n\
# Check for required environment variables\n\
if [ -z "$GEMINI_APIKEY" ]; then\n\
  echo "ERROR: GEMINI_APIKEY is not set. Agent will fail. Exiting."\n\
  exit 1\n\
fi\n\
\n\
# Create critical missing directories\n\
mkdir -p /root/.openclaw/agents/main/agent\n\
mkdir -p /root/.openclaw/agents/main/sessions\n\
mkdir -p /root/.openclaw/credentials\n\
\n\
# Remove auth-profiles.json if it exists to force env var usage\n\
rm -f /root/.openclaw/agents/main/agent/auth-profiles.json\n\
\n\
# Generate minimal config if not exists\n\
if [ ! -f /root/.openclaw/openclaw.json ]; then\n\
  cat > /root/.openclaw/openclaw.json << EOF\n\
{\n\
  "env": {\n\
    "GOOGLE_API_KEY": "${GEMINI_APIKEY}",\n\
    "GEMINI_API_KEY": "${GEMINI_APIKEY}",\n\
    "GOOGLE_GENERATIVE_AI_API_KEY": "${GEMINI_APIKEY}",\n\
    "PALM_API_KEY": "${GEMINI_APIKEY}"\n\
  },\n\
  "gateway": {\n\
    "mode": "local",\n\
    "bind": "lan",\n\
    "port": 18789,\n\
    "trustedProxies": ["0.0.0.0/0"],\n\
    "auth": {\n\
      "mode": "token",\n\
      "token": "${OPENCLAW_GATEWAY_PASSWORD}"\n\
    },\n\
    "controlUi": {\n\
      "allowInsecureAuth": true\n\
    }\n\
  },\n\
  "agents": {\n\
    "defaults": {\n\
      "model": {\n\
        "primary": "google/gemini-1.5-pro"\n\
      }\n\
    }\n\
  }\n\
}\n\
EOF\n\
fi\n\
\n\
# Fix permissions\n\
chmod 700 /root/.openclaw\n\
chmod 600 /root/.openclaw/openclaw.json\n\
chmod 600 /root/.openclaw/agents/main/agent/auth-profiles.json\n\
\n\
# Fix config if needed\n\
openclaw doctor --fix\n\
\n\
# Start OpenClaw gateway\n\
echo "Starting OpenClaw gateway on port 18789..."\n\
exec openclaw gateway run --port 18789\n\
' > /usr/local/bin/start-openclaw && chmod +x /usr/local/bin/start-openclaw

CMD ["/usr/local/bin/start-openclaw"]
```



脚本粘贴完成后，点击「Commit new file to main」提交，此时 Hugging Face Spaces 会自动开始构建和部署 OpenClaw，我们可以通过页面的「Log」查看构建进度。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186540125.png?tx)



## 四、配置环境变量：解决启动核心报错



提交 Dockerfile 后，若直接等待部署，大概率会出现运行错误，查看日志会显示「ERROR: GEMINI_APIKEY is not set」，这是因为 OpenClaw 运行依赖两个核心私密环境变量，必须完成配置才能正常启动，操作步骤如下：

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772186734853.png?tx)



### 设置环境变量

进入 Space 工作台，点击右侧的「Settings」，下拉页面找到「Variables and secrets」模块。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772187015709.png?tx)

点击「New secret」，依次添加两个环境变量：

- 变量名：`OPENCLAW_GATEWAY_PASSWORD`，值：自定义的访问密码（后续登录 OpenClaw 需要）；
- 变量名：`GEMINI_APIKEY`，值：Google Gemini 的 API 密钥（需自行在 Google 云平台申请）。

![img](https://imgoss.xgss.net/picgo-tx2026/QQ_1772187067071.png?tx)

两个变量添加完成后，Hugging Face 会自动重新开始构建和部署，此时只需等待部署状态变化即可。

查看日志：



## 六、查看部署状态，完成 OpenClaw 登录

1. 部署状态可通过 Space 页面的标识查看：「Building」为拉取镜像、构建环境阶段，若出错可在 Logs 的 Build 中排查；「Starting」为安装部署镜像阶段，报错可在 Logs 的 Container 中查看；当状态变为「Running」，说明部署完成。
2. 获取 OpenClaw 访问地址：部署完成后，可通过「Embed this Space」查看 Direct URL，格式为`https://[YourName]-openclaw.hf.space/`，也可直接点击页面的「App」进入。
3. 完成登录连接：进入 OpenClaw 页面后，在「Gateway Token」输入框中，填写之前配置的`OPENCLAW_GATEWAY_PASSWORD`值，点击「Connect」，当页面显示「Health OK」，说明已成功连接，可开始使用 OpenClaw 的各项功能。





## 七、温馨提示：谨慎使用，注意 Token 消耗

OpenClaw 功能强大，但有一个明显的特点 ——**Token 消耗速度极快**，在开源 AI Agent 圈更是被戏称为 “油老虎”，社区中不乏 “一天烧 $100+”“半天消耗 50M tokens” 的情况，大家在使用过程中一定要合理规划，避免不必要的 Token 浪费，且用且珍惜。

以上就是借助 Hugging Face 免费服务器部署 OpenClaw 的完整步骤，整个过程利用平台提供的免费资源，零成本就能玩转这款热门的开源 AI Agent。从环境搭建到配置部署，每一步的核心都是为了让 OpenClaw 在免费服务器上稳定运行，只要跟着步骤操作，就能顺利完成部署，解锁这只 “开源大龙虾” 的强大能力。